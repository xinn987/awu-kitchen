# 统一文本文件使用 LF，避免跨平台提交产生无意义的换行差异
* text=auto eol=lf

# Windows 批处理脚本保留 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf

# 常见二进制资源不参与换行转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.woff binary
*.woff2 binary
